
@model IEnumerable<MeteoMeter.Backend.DataModels.Reporting>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Reporting Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
				<th>
            @Html.DisplayNameFor(m => m.Id)
        </th>
				<th>
            @Html.DisplayNameFor(m => m.PublicationDate)
        </th>
				<th>
            @Html.DisplayNameFor(m => m.Content)
        </th>
				<th>
            @Html.DisplayNameFor(m => m.Type)
        </th>
				<th>
            @Html.DisplayNameFor(m => m.Results)
        </th>
				<th>
            @Html.DisplayNameFor(m => m.Views)
        </th>
				<th>
            @Html.DisplayNameFor(m => m.IdCard)
        </th>
		        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
				<th>
            @Html.DisplayFor(modelItem => item.Id)
        </th>
				<th>
            @Html.DisplayFor(modelItem => item.PublicationDate)
        </th>
				<th>
            @Html.DisplayFor(modelItem => item.Content)
        </th>
				<th>
            @Html.DisplayFor(modelItem => item.Type)
        </th>
				<th>
            @Html.DisplayFor(modelItem => item.Results)
        </th>
				<th>
            @Html.DisplayFor(modelItem => item.Views)
        </th>
				<th>
            @Html.DisplayFor(modelItem => item.IdCard)
        </th>
		        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
